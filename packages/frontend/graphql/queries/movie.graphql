query Movie($movieId: Int!) {
	movie(movieId: $movieId) {
		id
		title
		overview
		poster
		backdrop
		locked
		released
		runtime
		trailer
		rating
		genres {
			name
		}
		actors(take: 8) {
			role
			person {
				name
				thumbnail
			}
		}
	}
}
